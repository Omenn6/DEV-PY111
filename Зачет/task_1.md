Решение:
1. В цикле while на каждом шаге переменная делится на 1.7, поэтому цикл выполняется за O(log n)
2. Внутри цикла append работает за O(1), так что сложность не меняется
3. Метод out.merge_sort() сортирует полученный список за O(K log K), где длина списка K = log n

Ответ: O(log n * log(log n))
